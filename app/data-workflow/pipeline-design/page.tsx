import { ComingSoon } from "@/components/ComingSoon";

export default function PipelineDesignPage() {
  return <ComingSoon title="Pipeline Design" description="End-to-end ML pipelines: from raw data to deployment-ready models." parentHref="/data-workflow" parentTitle="Data Workflow" />;
}
