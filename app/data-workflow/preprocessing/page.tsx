import { ComingSoon } from "@/components/ComingSoon";

export default function PreprocessingPage() {
  return <ComingSoon title="Preprocessing" description="Scaling, normalization, encoding, and transformations — preparing data for models." parentHref="/data-workflow" parentTitle="Data Workflow" />;
}
