import { ComingSoon } from "@/components/ComingSoon";

export default function ClassificationMetricsPage() {
  return (
    <ComingSoon
      title="Classification Metrics"
      description="Accuracy, Precision, Recall, F1, AUC-ROC, Log Loss — navigating the tradeoffs."
      parentHref="/evaluation"
      parentTitle="Evaluation"
    />
  );
}
