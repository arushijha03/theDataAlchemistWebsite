import { ComingSoon } from "@/components/ComingSoon";

export default function OptimizationPage() {
  return (
    <ComingSoon
      title="Optimization"
      description="Gradient descent, convexity, loss functions, and constrained optimization — how models actually learn."
      parentHref="/foundations"
      parentTitle="Foundations"
    />
  );
}
