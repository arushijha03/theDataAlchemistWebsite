import { ComingSoon } from "@/components/ComingSoon";

export default function LinearAlgebraPage() {
  return (
    <ComingSoon
      title="Linear Algebra"
      description="Vectors, matrices, eigenvalues, projections, and decompositions — the language of machine learning."
      parentHref="/foundations"
      parentTitle="Foundations"
    />
  );
}
